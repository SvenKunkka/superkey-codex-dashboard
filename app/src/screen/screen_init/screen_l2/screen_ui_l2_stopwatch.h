/**
 * @file screen_ui_l2_stopwatch.h
 * @brief 秒表UI模块 - 计时器功能
 * 
 * 秒表功能:
 * - L1入口: 计时器图标入口
 * - L2界面: 计时显示/状态/控制
 * 
 * 页面布局:
 * - 左屏: 秒表标题 + 状态 + 开始/暂停提示
 * - 中屏: 计时显示(MM:SS.d) + 重置提示
 * - 右屏: 计时器图标
 * 
 * 控制说明:
 * - 按键1: 开始/暂停
 * - 按键2: 重置
 */

#ifndef SCREEN_UI_L2_STOPWATCH_H
#define SCREEN_UI_L2_STOPWATCH_H

#include "lvgl.h"

#ifdef __cplusplus
extern "C" {
#endif

/*******************************************************************************
 * L1 入口面板构建
 ******************************************************************************/

/**
 * @brief 构建计时器入口面板
 * @param parent 父容器
 * 
 * 包含:
 * - 计时器图标入口
 * - "计时器"提示文字
 */
void l2_stopwatch_build_entrance_panel(lv_obj_t *parent);

/*******************************************************************************
 * L2 界面构建
 ******************************************************************************/

/**
 * @brief 构建L2秒表界面
 * @param screen 屏幕对象
 * 
 * 三屏布局:
 * - 左屏: 秒表标题 + 状态标签 + 开始/暂停提示
 * - 中屏: 计时显示 + 重置提示
 * - 右屏: 计时器图标
 */
void l2_stopwatch_build_page(lv_obj_t *screen);

/*******************************************************************************
 * 更新函数
 ******************************************************************************/

/**
 * @brief 更新秒表显示
 * @return 0成功，非0失败
 * 
 * 更新内容:
 * - 计时显示(分:秒.十分之一秒)
 * - 状态标签(待机/运行中/已暂停)
 */
int l2_stopwatch_update_display(void);

#ifdef __cplusplus
}
#endif

#endif /* SCREEN_UI_L2_STOPWATCH_H */
