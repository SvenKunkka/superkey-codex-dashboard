/**
 * @file screen_ui_l2_tomato.h
 * @brief 番茄钟UI模块 - 番茄工作法计时器
 * 
 * 番茄钟功能:
 * - L1入口: 番茄钟图标入口
 * - L2界面: 倒计时/模式/状态/统计显示
 * 
 * 页面布局:
 * - 左屏: 模式(专注/短休息/长休息) + 状态 + 轮次
 * - 中屏: 倒计时显示 + 进度条
 * - 右屏: 今日完成数 + 连续数
 * 
 * 模式说明:
 * - 专注模式: 25分钟工作时间
 * - 短休息: 5分钟休息
 * - 长休息: 15分钟休息(每4个番茄后)
 */

#ifndef SCREEN_UI_L2_TOMATO_H
#define SCREEN_UI_L2_TOMATO_H

#include "lvgl.h"

#ifdef __cplusplus
extern "C" {
#endif

/*******************************************************************************
 * L1 入口面板构建
 ******************************************************************************/

/**
 * @brief 构建番茄钟入口面板
 * @param parent 父容器
 * 
 * 包含:
 * - 番茄钟图标入口
 * - "番茄钟"提示文字
 */
void l2_tomato_build_entrance_panel(lv_obj_t *parent);

/*******************************************************************************
 * L2 界面构建
 ******************************************************************************/

/**
 * @brief 构建L2番茄钟界面
 * @param screen 屏幕对象
 * 
 * 三屏布局:
 * - 左屏: 模式标签 + 状态标签 + 轮次显示
 * - 中屏: 倒计时显示 + 进度条
 * - 右屏: 今日统计 + 连续完成数
 */
void l2_tomato_build_page(lv_obj_t *screen);

/*******************************************************************************
 * 更新函数
 ******************************************************************************/

/**
 * @brief 更新番茄钟显示
 * @return 0成功，非0失败
 * 
 * 更新内容:
 * - 模式标签和颜色
 * - 倒计时显示
 * - 进度条
 * - 状态标签
 * - 统计数据
 * - 轮次显示
 */
int l2_tomato_update_display(void);

#ifdef __cplusplus
}
#endif

#endif /* SCREEN_UI_L2_TOMATO_H */
